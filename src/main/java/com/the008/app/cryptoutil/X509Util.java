package com.the008.app.cryptoutil;

import java.io.File;
import java.security.PrivateKey;
import java.security.PublicKey;

public class X509Util {

    public static void main(String[] args) throws Exception {
//        String pk = "ssl/private.key.pem";
//        RSAKeyUtil keys = new RSAKeyUtil();
//        PrivateKey privateKey = keys.readPrivateKey(new File(pk));
//        RSAUtil rsa_pv = new RSAUtil(privateKey);
//        PublicKey publicKey = keys.generatePublicKey(privateKey);
//        RSAUtil rsa_pb = new RSAUtil(publicKey);
//        byte[] encrypted;
//        File f_enc = new File("ssl/secret.txt");
//        encrypted = rsa_pb.encrypt(FileIO.read(f_enc));
//        FileIO.write("ssl/secret.encrypted", encrypted);
//        byte[] unencrypted = rsa_pv.decrypt(encrypted);
//        FileIO.write("ssl/secret.plain", unencrypted);

    }

}
